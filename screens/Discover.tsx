import { Text } from "react-native-paper";
import Animated from "react-native-reanimated";

export default function Discover() {
  return (
    <Animated.View>
      <Text>{"chupapi muñaño ".repeat(500)}</Text>
    </Animated.View>
  );
}
